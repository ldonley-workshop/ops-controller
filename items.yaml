removeStrategy:
  rbac: SYNC
  items: NONE
items:
- kind: folder
  name: controller-jobs
  description: 'These are my controller jobs'
  properties:
  - kind: envVars
    vars:
      GITHUB_ORGANIZATION: ${GITHUB_ORGANIZATION}
